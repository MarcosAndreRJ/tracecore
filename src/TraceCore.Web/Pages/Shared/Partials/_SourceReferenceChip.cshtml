@model (string SourceType, string Identifier)

<span class="tc-source-chip" title="Referência de Origem: @Model.SourceType">
    <i class="bi bi-tag" aria-hidden="true"></i>
    <span class="text-muted">@Model.SourceType:</span>
    <span class="fw-semibold">@Model.Identifier</span>
</span>
